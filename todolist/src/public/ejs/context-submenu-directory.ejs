<script type="text/template" id="ctmSubDirectoryTmpl">
    <% if(dirs.length){ %>
    <% for (var i in dirs) { %>
<li class="context-menu-item menuItem dir-item" item-id="<%=dirs[i].id%>">
    <span class="context-menu-icon">   </span>
    <span class="label"><%=dirs[i].name%></span>
</li>
    <% } %>
    <% } %>
</script>